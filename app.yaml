name: fcoin-agent
region: nyc
static: false
http_port: 8080
build_command: pip install --no-cache-dir -e ".[sse]"
run_command: python -m src --transport sse --port 8080
health_check:
  path: /health
  interval: 30
  timeout: 5
  protocol: http
envs:
  - key: PORT
    value: "8080"
