services:
  - type: web
    name: fcoin-agent
    runtime: python
    buildCommand: pip install -e ".[sse]"
    startCommand: python -m src --transport sse --port $PORT
    healthCheckPath: /health
    envVars:
      - key: PORT
        value: 8080
